namespace Codecool.Logging
{
    /// <summary>
    /// Represents different log levels to use for logging events
    /// with different severity.
    /// </summary>
    public enum LogLevel
    {
        INFO,
        DEBUG,
        WARNING,
        ERROR,
    }
}